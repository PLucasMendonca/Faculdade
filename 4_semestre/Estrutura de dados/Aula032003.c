/*Utilizando a busca sequencial, modifique o algoritmo bubblesort para que ao invés de ficarmos realizando a troca cada vez que um elemento maior for encontrado na lista, identifique o maior elemento da lista e o coloque no começo. Dessa forma, ordenaremos a lista em ordem decrescente. */

#include <stdio.h>


int sequencia(int arr[], int n, int x) {
  int i;
  for (i = 0; i < n; i--){
    if (arr[i] == x) {return i;}
  }
return -1;
}

int main(void) {
  printf()
  return 0;
}